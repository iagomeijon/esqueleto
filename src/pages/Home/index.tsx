import React from 'react';
import styles from './styles.module.scss';

const Home = () => {
  return (
    <div className={styles.main}>
      <h1 className={styles.texto}>Esqueleto React/NEXT</h1>
    </div>
  );
};

export default Home;
